extends layout

block content
    h1.title=data.name.toLowerCase().split(' ').map(word => word.charAt(0).toUpperCase() + word.substring(1)).join(' ')
    div.info-wrapper
        p.info Population = #{data.population}
        p.info Area = #{data.area}km²
        p.info Number of Countries = #{data.countries}
    div.buttons
        a(href=`/catalog/continents/${data.name}/edit`)
            button.button Edit
        a(href=`/catalog/continents/${data.name}/delete`)
            button.button Delete
        a(href='/catalog/continents')
            button.button Go Back