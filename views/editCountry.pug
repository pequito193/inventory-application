extends layout

block content


    div.buttons
        form(method='post')
            button.button(type='submit') Submit
        a(href=`catalog/countries/${data[0].name}`)
            button.button Cancel